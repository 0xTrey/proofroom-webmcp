# ProofRoom demo script

> Local preparation only. No official Devpost draft exists locally, and nothing has been submitted.

## Timing budget

| Measure | Budget |
| --- | --- |
| Spoken words | 272 |
| Assumed speaking rate | 135 words per minute |
| Simple arithmetic speech time | 2:01 (272 / 135 × 60 = 120.9 seconds) |
| Measured generated narration | Pending independent timing gate |
| Unvoiced interaction and pause allowance | 34 to 44 seconds by simple arithmetic |
| Target final duration | 2:35 to 2:45, ending by 2:45 |

The spoken-word count excludes headings, timestamps, on-screen action cues, and the
local-preparation notice.

## 0:00 to 0:17

On screen: Open the live `#product` surface. Keep the URL, primary headline, fictional-data notice,
and WebMCP status visible.

> B2B sites force agents to scrape disconnected claims. ProofRoom gives the buyer and agent one room for evidence, assumptions, and decisions. Instead of asking a model to trust marketing language, the page exposes structured state and deterministic rules. Northstar and Meridian Bank are fictional demo content.

## 0:17 to 0:36

On screen: Select `Stage fictional Meridian Bank draft`. Pause on the pending proposal and its exact
fields. Select `Approve buyer context`.

> Buyer context is staged, not silently applied. I inspect the fields and revision, then approve them here. That visible human approval makes the context authoritative and personalizes the product without hiding the security gap.

## 0:36 to 1:00

On screen: Open `Evaluation`. Select `Apply fictional review set`. Show the supported Salesforce
requirement and unknown `EU data residency`, then inspect and close untrusted `ev_011`.

> Salesforce is supported because evidence covers every hard condition. EU residency stays unknown because the catalog cannot prove it. ProofRoom keeps an instruction-styled testimonial quarantined as untrusted data. It cannot prove security or control the workflow.

## 1:00 to 1:18

On screen: Open `Decision` with one saved CFO brief pre-positioned. Select `Preview calculation`,
then show the saved CFO brief and the open CISO question. Do not claim the brief was created live.

> ROI exposes cost, payback, budget, and every assumption. The saved CFO brief cites the economics. The CISO view keeps EU residency open. Stakeholder synthesis cannot outrun the evidence.

## 1:18 to 1:40

On screen: Select `Fill canonical not-ready draft`, then `Stage proposal`. Pause on both blockers.
Select `Approve decision` and show the receipt.

> The proposal is not ready because EU residency is unknown and SSO is partial. The agent can stage that recommendation, but cannot approve it. I make the second human approval here, and the receipt records what became authoritative.

## 1:40 to 2:05

On screen: Show the native verifier receipt or live DevTools discovery from real
`document.modelContext`. Keep the nine names legible.

> Native Chrome discovered nine tools through real `document.modelContext`, before and after reload, with no shim. Two tool executions moved revision zero to one and persisted the proposal. They cover room state, evidence, requirements, ROI, stakeholder briefs, buyer context, and decision proposals.

## 2:05 to 2:45

On screen: Return to the activity register, then briefly show the architecture diagram in the public
README, then hold on the proof table with the live URL and repository legible.

> UI and WebMCP share one RoomActions layer, so the same rules and activity ledger govern both paths. Verification passed 423 unit and component tests, 38 end-to-end tests, 48 accessibility checks, and 12 evals. The live app and public MIT repository are linked here. Natural-language selection was not run.
