# ProofRoom demo script

> Local preparation only. Authenticated Devpost project `1402028` exists as an Untitled, empty
> pre-draft shell. It has no video and has not been submitted.

## Timing budget

| Measure | Budget |
| --- | --- |
| Spoken words | 262 |
| Assumed speaking rate | 135 words per minute |
| Simple arithmetic speech time | 1:56 (262 / 135 × 60 = 116.4 seconds) |
| Measured generated narration | 129.0 seconds (`say -r 135` on the current spoken block) |
| Unvoiced interaction and pause allowance | 36.0 seconds before the 2:45 stop |
| Target final duration | 2:35 to 2:45, ending by 2:45 |

The spoken-word count excludes headings, timestamps, on-screen action cues, and the
local-preparation notice. The measured narration gate applies to the current script text only.

## 0:00 to 0:12

On screen: Open `/`. Keep the primary headline, definition copy, fictional EU example card, three-step
decision chain, and `Open the fictional review` visible.

> B2B buying pages scatter claims across marketing copy. ProofRoom lets a browser agent check vendor claims against source records while the buyer controls priorities and the final decision. Northstar and Meridian Bank are fictional demo content.

## 0:12 to 0:30

On screen: Select `Open the fictional review`. Select `Review the sample buyer profile`. Pause on the
exact fields. Select `Use this buyer profile`.

> The live room opens immediately. Buyer priorities are staged first, not silently applied. I review the sample profile and approve it here. That visible human approval makes the priorities authoritative without hiding the security boundary.

## 0:30 to 0:55

On screen: Select `Check evidence`, then `Run the sample evidence check`. Show supported Salesforce
integration and unknown `EU data residency`, then inspect and close untrusted `ev_011`.

> Salesforce is supported because eligible records cover every hard condition. EU residency stays unknown because no record proves an EU commitment. ProofRoom quarantines an instruction-styled testimonial as untrusted data. It cannot prove security or control the workflow.

## 0:55 to 1:15

On screen: Select `Review decision` with one saved CFO brief pre-positioned. Select `Preview
calculation`, then show the saved CFO brief and the open CISO question. Do not claim the brief was
created live.

> ROI exposes subscription cost, payback, budget, and every assumption. The saved CFO brief cites the economics. The CISO view keeps EU residency open. Stakeholder synthesis cannot outrun the evidence.

## 1:15 to 1:38

On screen: Select `Prepare the sample not-ready recommendation`, then `Prepare recommendation`. Pause
on both blockers. Select `Approve recommendation` and show the receipt.

> The recommendation is not ready because EU residency is unknown and SSO is partial. The agent can stage that proposal, but cannot approve it. I make the second human approval here, and the receipt records what became authoritative.

## 1:38 to 2:05

On screen: Show the native verifier receipt or live DevTools discovery from real
`document.modelContext`. Keep the nine names legible.

> Native Chrome discovered nine tools through real `document.modelContext`, before and after reload, with no shim. Two tool executions moved revision zero to one and persisted the proposal. They cover room state, evidence, requirements, ROI, stakeholder briefs, buyer context, and decision proposals.

## 2:05 to 2:45

On screen: Return to the activity register, then briefly show the architecture diagram in the public
README, then hold on the proof table with the repository legible.

> UI and WebMCP share one RoomActions layer, so the same rules and activity ledger govern both paths. Public baseline checks passed 423 unit and component tests, 38 end-to-end tests, 48 accessibility checks, and 12 evals. The local candidate is not deployed. Natural-language selection was not run.
