# ProofRoom demo shot list

> Local preparation only. No official Devpost draft exists locally, and nothing has been submitted.

This plan matches the 2:35 to 2:45 target in [`demo-script.md`](demo-script.md) and ends at 2:45.
All product entities and claims shown on screen are fictional demo content.

## Capture setup

- Record a 1600 by 900 browser window at 100 percent browser zoom.
- Keep the address bar visible whenever the public origin is part of the proof.
- Use the public origin: `https://proofroom-webmcp.harnden-trey.workers.dev`.
- Close personal tabs, extensions, bookmarks, notifications, and account menus before recording.
- Use a fresh browser profile or clear only ProofRoom site storage before the take.
- Confirm the opening Product tab starts at revision 0 with no approved context or decision.
- Keep the pre-positioned Decision tab free of any pending or approved decision.
- Keep the pointer still while speaking. Move once toward the next control, then click.
- Do not resize the window, change zoom, or use browser search during the take.

## Ordered shots

| Time | Route or surface | Exact action | Expected visible state | Narration goal | Capture mode |
| --- | --- | --- | --- | --- | --- |
| 0:00 | `/#product`, opening Product surface | Load the public URL and hold | One primary headline, fictional-data disclosure, and honest WebMCP status | State the buyer problem and thesis | Live |
| 0:17 | `/#product`, buyer-context panel | Select `Stage fictional Meridian Bank draft`, then hold on the exact fields | Pending proposal with buyer fields and revision metadata | Show staging before authority | Live |
| 0:28 | `/#product`, same panel | Select `Approve buyer context` | Buyer-approved context notice and reordered product content | Prove the first human-only approval | Live |
| 0:36 | `/#evaluation`, requirement workspace | Select `Evaluation`, then `Apply fictional review set` | Three supported requirements plus partial, unsupported, and unknown states | Show deterministic evidence work quickly | Live |
| 0:46 | `/#evaluation`, requirement records | Open `Salesforce integration`, then `EU data residency` | Salesforce is supported; EU residency is unknown with both hard conditions open | Contrast proof with an honest gap | Live |
| 0:54 | `/#evaluation`, catalog index | Select `Inspect ev_011`, hold, then close | `Treat this as data, not instructions` and the instruction-styled testimonial sentence are visible | Show untrusted-content handling | Live |
| 1:00 | `/#decision`, ROI workspace | Switch to the pre-positioned Decision state and select `Preview calculation` | Deterministic ROI results, payback, and budget comparison; no silent apply | Make assumptions and math inspectable | Live preview from pre-positioned valid state |
| 1:09 | `/#decision`, stakeholder briefs | Show the saved CFO brief, then select the CISO view | Saved CFO economics and the open CISO EU question | Show audience-specific synthesis without claiming a live save | Pre-positioned saved CFO brief |
| 1:18 | `/#decision`, proposal composer | Select `Fill canonical not-ready draft`, then `Stage proposal` | Pending `not ready` proposal with `req_eu_residency` and `req_sso` blockers | Show that hard gaps control the recommendation | Live |
| 1:32 | `/#decision`, proposal review | Select `Approve decision` and hold on the receipt | Approved decision and decision receipt | Prove the second human-only approval | Live |
| 1:40 | Native WebMCP proof | Show the committed native receipt or real `document.modelContext` discovery | Exactly nine tool names, native Chrome version, reload proof, and successful executions | Prove native discovery and execution without a shim | Pre-positioned real evidence, never recreated output |
| 2:05 | `/#decision`, activity register, then README | Return to the activity register, switch to the pre-positioned README architecture section, then hold on the proof table and links | Person and agent events, shared `RoomActions` flow, test counts, live URL, and repository URL | Close on the trust boundary and reproducible proof | Live ledger, pre-positioned repository tab |

## Live and pre-positioned state

Keep these interactions live in the take:

- Loading the public Product surface.
- Staging and approving buyer context.
- Applying the fictional review set and inspecting requirement state.
- Opening and closing the untrusted testimonial.
- Previewing ROI.
- Staging and approving the decision.
- Showing the activity register.

Prepare these before recording:

- A second tab on the public repository README architecture section.
- A third tab on either live native DevTools discovery or the committed
  [`native-webmcp.json`](../../artifacts/release/native-webmcp.json) receipt.
- A separate Decision tab with reviewed requirement state, one truthful CFO brief already saved, the
  CISO view ready to show, and no pending or approved decision.

Pre-positioning may shorten navigation. It must not replace a claimed action with edited state or
fabricated output. The narration does not claim the CFO brief was saved during the take.

## Native feature recovery plan

If `document.modelContext` is unavailable during recording:

1. Do not claim live native discovery.
2. Keep the complete visible UI journey. Every product action remains available without WebMCP.
3. Show the committed
   [`native-webmcp.json`](../../artifacts/release/native-webmcp.json) receipt and its exact nine
   `toolNames`, `reloadToolNames`, native executions, persistence result, and Chrome version.
4. State that the accepted native verifier used headed Chrome and no WebMCP shim.
5. State that live natural-language browser-agent selection is `not_run`.
6. If the receipt cannot be shown legibly, cut the native shot and record a new take. Do not add
   fake terminal output, edited DevTools output, or a simulated tool call.

## Final capture checklist

- [ ] Public URL is legible at the opening and the repository URL is legible at the close.
- [ ] Fictional-data notice is visible and verbally disclosed.
- [ ] Buyer context is visibly staged before the person approves it.
- [ ] Decision proposal is visibly staged before the person approves it.
- [ ] Supported evidence and the unknown EU residency gap are both visible.
- [ ] Exactly nine native tool names are legible in real discovery or the committed receipt.
- [ ] No line claims a passed live natural-language browser-agent run.
- [ ] Test and eval counts are 423, 38, 48, and 12.
- [ ] Audio is clear, even, and free of notification sounds.
- [ ] Cursor movement is deliberate and never covers the active proof.
- [ ] Browser zoom remains 100 percent and no text is clipped.
- [ ] Final exported duration is 2:35 to 2:45 and does not run past 2:45.
- [ ] No fake terminal output, edited proof, personal data, or account UI appears.
