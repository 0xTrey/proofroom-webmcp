# Item 8 decision audit

Generated by:

```text
npm run capture:visual:decision
```

Tracked milestone evidence:

- `initial-decision-390.png`, 390 by 900. The untouched decision desk before context,
  evidence, brief, ROI, or proposal actions.
- `initial-decision-1600.png`, 1600 by 900. The untouched wide decision desk and human
  approval boundary.
- `roi-preview-390.png`, 390 by 900. A valid edited and unapplied ROI preview with model
  results, buyer target context, and an above-budget warning.
- `roi-preview-1600.png`, 1600 by 900. The same edited and unapplied preview across the
  wide commercial-model layout.
- `proposal-review-390.png`, 390 by 900. The canonical not-ready proposal review with the
  visible `req_eu_residency` unknown blocker and `req_sso` partial blocker.
- `proposal-review-1600.png`, 1600 by 900. The same exact canonical proposal in the wide
  decision desk, including both blocking requirement rows.
- `approved-receipt-390.png`, 390 by 900. The approved receipt visibly separates kind,
  proposal ID, payload digest, approved revision, issued timestamp, and safe summary.
- `approved-receipt-1600.png`, 1600 by 900. The same receipt fields remain visible in the
  wide metadata layout.

Screenshot writes are gated behind `UPDATE_VISUAL_AUDIT=1`. Normal E2E runs assert these
states without rewriting this tracked evidence.
